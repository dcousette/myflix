%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @friendships.each do |friendship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest("#{friendship.leader.email_address}".downcase)}?s=40")
            = link_to friendship.leader.full_name
          %td.extra-padding= friendship.leader.queue_item_count
          %td.extra-padding= friendship.leader.following_friendships.count
          %td.extra-padding
            = link_to friendship, method: :delete do
              %i.glyphicon.glyphicon-remove